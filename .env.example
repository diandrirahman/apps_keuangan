# Server
PORT=3000
CLIENT_ORIGIN=http://localhost:5173

# Gemini (optional: endpoints will return 503 until configured)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Frontend (required only when the API is hosted on another domain)
VITE_API_URL=
